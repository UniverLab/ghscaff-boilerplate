# secrets.toml — secrets required by this template
# ghscaff reads this and prompts the user to configure each as a GitHub repo secret.

[[secrets]]
name = "PYPI_API_TOKEN"
description = "API token for publishing to PyPI (get it at https://pypi.org/manage/account/token/)"
required = true
